import {chromium} from 'playwright';
const browser=await chromium.launch();const page=await browser.newPage({viewport:{width:Number(process.env.PROBE_WIDTH??390),height:844}});await page.goto('http://127.0.0.1:4173/',{waitUntil:'domcontentloaded'});await page.waitForLoadState('networkidle',{timeout:1500}).catch(()=>{});await page.screenshot({path:'docs/evidence/final-public/menu-probe.png',fullPage:true});await page.getByRole('button',{name:'開啟選單'}).first().click();await page.waitForTimeout(1000);console.log(JSON.stringify(await page.evaluate(()=>({expanded:document.querySelector('[aria-controls="mobile-drawer"]')?.getAttribute('aria-expanded'),navs:Array.from(document.querySelectorAll('nav')).map(e=>({label:e.getAttribute('aria-label'),html:e.outerHTML.slice(0,120),inert:!!e.closest('[inert]')}))})),null,2));console.log('MATCHES',await page.getByRole('navigation',{name:'主選單'}).count());console.log(await page.locator('#mobile-drawer').ariaSnapshot());await browser.close();
