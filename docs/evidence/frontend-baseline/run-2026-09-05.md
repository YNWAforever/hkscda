# Frontend baseline run — 2026-09-05

Baseline: `codex/completion-20260905` at `20c168459a90c5c92093659a18b139a994451470`.

## Commands and outcomes

- Local fixture: `node scripts/ci/supabase-fixture.mjs` with the documented placeholder Supabase variables. Port 54329 returned HTTP 200.
- Local preview: `PORT=4173 bun run preview` with the same four placeholder variables. `/help` returned HTTP 200.
- Probe: `node docs/evidence/frontend-baseline/reproduce-wave1.mjs`.
  - `/help` loaded and `artifacts/help-mobile.png` was captured.
  - The probe then timed out after 30 seconds waiting for `#mobile-drawer` after clicking the visible `開啟選單` trigger on `/`.
  - The same timeout occurred on retry, before either footer CTA assertion.
  - Because this probe version writes JSON after all scenarios, no final JSON was produced. Exact hydration-console and post-CTA inert/focus browser evidence remains incomplete.
- Repository verifier: `MODE=brand BASE_URL=http://127.0.0.1:4173 OUTPUT_DIR=docs/evidence/frontend-baseline/brand-verifier bun run verify:brand`; interrupted after roughly 90 seconds without a result to avoid delaying the baseline.
- Cleanup: stopped the fixture process, preview parent and verified preview child. Ports 4173 and 54329 had no listeners afterward.

## Source-confirmed revalidation

- `src/routes/help.tsx`: loader calls `ensureQueryData(publicFaqsQueryOptions())`; `HelpPage` separately calls `useQuery(publicFaqsQueryOptions())` and defaults missing data to `[]`. This preserves the audited mechanism for a server/client first-render count mismatch.
- `src/components/site/Header.tsx`: the drawer effect applies body scroll lock, background `inert`, focus trapping and cleanup. Home/submenu links call `setDrawerOpen(false)`; footer links to `/animals/cat` and `/donate` omit it. This preserves the audited mechanism for navigation with the drawer overlay and locks retained.

The named frontend companion plan was not supplied. No production code was modified.
