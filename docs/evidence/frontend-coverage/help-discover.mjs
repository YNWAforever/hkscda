import{chromium}from'playwright';const b=await chromium.launch();const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://127.0.0.1:4173/help',{waitUntil:'networkidle'});console.log(await p.locator('button,a').evaluateAll(es=>es.filter(e=>e.offsetParent).map(e=>({text:e.textContent?.trim(),label:e.getAttribute('aria-label'),href:e.getAttribute('href'),expanded:e.getAttribute('aria-expanded')}))));await p.getByRole('button',{name:'開啟 HKSCDA 小幫手'}).click();console.log('dialogs',await p.locator('[role=dialog]').evaluateAll(es=>es.map(e=>({text:e.textContent?.slice(0,300),label:e.getAttribute('aria-label'),labelledby:e.getAttribute('aria-labelledby')}))));await b.close();
